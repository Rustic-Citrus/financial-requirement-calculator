{
  "tasks": {
    "dev": "deno run --watch main.ts",
    "coverage-report": "deno test --coverage && deno coverage --lcov --output=coverage/cov.lcov coverage/ && genhtml -o coverage/html_cov coverage/cov.lcov"
  },
  "imports": {
    "@std/assert": "jsr:@std/assert@1",
    "@std/testing": "jsr:@std/testing@^1.0.3"
  }
}
